//
//  main.cpp
//  odevConditions2
//
//  Created by Tugba on 17.03.2019.
//  Copyright © 2019 Tugba. All rights reserved.
//

#include <iostream>
#include <math.h> // bazı matematik işlem sembollerini içeren kütüphanedir. Eğer bunu ınclude etmezsek örneğin; karekoku hesaplayamayız.

using namespace std;
int main() {
    // insert code here...
    
    /*
     std::cout << "işin bitme suresini çözen kod\n";
    
    int kaç_işci, kaç_gun;
    
    cout <<"Bir işçi bir işi kaç gunde bıtırmektedir?"<< endl;
    cin>>kaç_gun;
    
    cout <<"Toplam kaç işçi çalışacak?"<< endl;
    cin>>kaç_işci;
    
    cout <<"İşin bitme süresi:" << float(kaç_gun/kaç_işci) << " gündür." << endl;
    
    */

    /*
     cout<<"hipotenus hesaplayan kod"<<endl;
    int dik_kenar,dik_kenar1,hıpotenus;

    cout<<"üçgenin bir dik kenarını giriniz."<<endl;
    cin>>dik_kenar;
    
    cout<<"üçgenin bir diğer dik kenarını giriniz."<<endl;
    cin>>dik_kenar1;
    
    hıpotenus = sqrt(dik_kenar1*dik_kenar1 + dik_kenar*dik_kenar);
    
    cout<<"hipotenusDeğeri:"<<  hıpotenus <<endl;
    */
    
    /*
     cout<<"dik üçgenin alanını ve cevresını hesaplayan kod"<<endl;
    int kenar,kenar1,alan,çevre,yukseklık;
    
    cout<<"üçgenin bir kenarını giriniz."<<endl;
    cin>>kenar;
    
    cout<<"üçgenin diğer bir kenarını giriniz."<<endl;
    cin>>kenar1;
    
    yukseklık=sqrt(kenar1*kenar1 + kenar*kenar);
    alan=(kenar*kenar1)/2;
    çevre=kenar1+kenar+yukseklık;
    
    cout<<"üçgenın yukseklıgı:" << yukseklık <<endl;
    cout<<"üçgenin alanı:"<< alan <<endl;
    cout<<"üçgenin çevresi:"<< çevre <<endl;
    */
    
    /*
     cout<<"yol hız problemleri- süreyi hesaplayan kod"<<endl;
    int yol,hız;

    
    cout<<"İller arasındakı mesafeyi giriniz.:" <<endl;
    cin>>yol;
    cout<<"gideceğiniz iller arasındaki mesafe:"<< yol << "km dir."<<endl;
    
    cout<<"Aracınızın hızını giriniz:"<<endl;
    cin>>hız;
    cout<<"Aracınızın hızı:"<< hız << "km/h dır."<<endl;
    
    int saat = yol/hız;
    float dakikakismi = float(yol/hız) - int(yol/hız);
    int dakika = dakikakismi * 60;
    
    cout<<"tahmini Varış sureniz :" << saat << " saattir " ;
    cout<<"ve "<< dakika << " dakikadır." <<endl;
     */
    
   /*
    cout<<"dik üçgen kontrolü"<<endl;
    
    int a,b,c;
    
    cout<<"dik üçgenin kenarlarını giriniz."<<endl;
    
    cin>>a;
    cin>>b;
    cin>>c;
    
    if (a*a + b*b == c*c || a*a + c*c == b*b || c*c + b*b == a*a) {
        cout<<"dik üçgendir."<<endl;
    }
    else{
        cout<<"dik üçgen değildir."<<endl;
    }
    
    */    
    
    
    return 0;
}
